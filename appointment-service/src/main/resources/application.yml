server:
  port: 4100

spring:
  application:
    name: APPOINTMENT-SERVICE


eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
